apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  # creationTimestamp: null
  labels:
    io.kompose.service: auth-postgres-data
  name: auth-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
# status: {}
